import 'package:flutter/material.dart';
import 'package:myproject/helper/db.dart';
//import 'package:mysql1/mysql1.dart';

//final db = DB();
/*final db = new DB();

Results? users_data;

void fillUser() async {
  //users_data = await db.getData('select');
  var settings = ConnectionSettings(
    host: 'localhost',
    port: 84,
    user: 'root',
    db: 'myStore',
    password: '',
  );
  print('connected');
  MySqlConnection.connect(settings).then((value) {
    print(value);
  });
  print('connected 2 ');
}

class RunDB extends StatelessWidget {
  const RunDB({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    fillUser();
    return Container();
  }
}*/
